import 'package:flutter/material.dart';

class UniwersalStatisticsView extends StatelessWidget {
  const UniwersalStatisticsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
